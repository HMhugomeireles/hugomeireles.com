import React, { Fragment } from 'react'
import styled from 'styled-components'

const Hamburger = () => (
    <Fragment>
      Menu
    </Fragment>
)

export default Hamburger;

const StyledLogo = styled(Hamburger)`
    
`;