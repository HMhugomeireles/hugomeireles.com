import styled from "styled-components"

export const HeaderWrapper = styled.header`
  position: fixed;
  top: 0;
  left: 50%;
  max-width: 1024px;
  width: 100%;
  z-index: 99;

  transform: translateX(-50%);
`
